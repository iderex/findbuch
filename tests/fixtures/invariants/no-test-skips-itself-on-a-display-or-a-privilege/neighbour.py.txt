"""The same test with nothing to skip for.

The backend is selected before the plotting library is imported, so the figure
is produced without a display and the assertion is made on every machine. The
skip is gone rather than made conditional on something else.
"""

import unittest


class ThePlotIsProduced(unittest.TestCase):
    def test_the_figure_renders(self):
        self.assertTrue(True)
